[user]
    name = "Nora Ballard"
    email = "4589305+Nora-Ballard@users.noreply.github.com"

[core]
    editor = nvim
[pager]
    branch = false

[credential]
        helper = cache --timeout=43200

[diff]
    tool = nvimdiff
[difftool]
    prompt = false
[difftool "nvimdiff"]
    cmd = "nvim -d \"$LOCAL\" \"$REMOTE\""
[merge]
    tool = nvimdiff
[mergetool]
    prompt = false
[mergetool "nvimdiff"]
    cmd = "nvim -d \"$LOCAL\" \"$REMOTE\" \"$MERGED\" -c 'wincmd w' -c 'wincmd J'"
[color]
    status = auto
    diff = auto
    branch = auto
    interactive = auto
    grep = auto
    ui = auto
[hub]
    protocol = https
[github]
    user = nora-ballard
    
[include]
        path = ~/.gitconfig_local
